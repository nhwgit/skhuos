#ifndef __TYPE_H___
#define __TYPE_H___

typedef enum { FALSE, TRUE } bool;

typedef unsigned char	BYTE;
typedef unsigned short	WORD;
typedef unsigned int	DWORD;
typedef unsigned long long	QWORD; // 32비트 모드에서도 8바이트 보장

#define NULL 0
#endif
